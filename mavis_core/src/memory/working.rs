// mavis_core/src/memory/working.rs
// Working Memory: active context window. Fast, in-memory, volatile.

pub struct WorkingMemory;

impl WorkingMemory {
    pub fn new() -> Self {
        Self
    }
}